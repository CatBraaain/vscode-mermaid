{
    "configurations": [
        {
            "name": "Extension",
            "type": "extensionHost",
            "request": "launch",
            "preLaunchTask": "npm: yaml2json",
            "runtimeExecutable": "${execPath}",
            "args": [
                "${workspaceFolder}/tests",
                "--extensionDevelopmentPath=${workspaceFolder}",
                "--disable-extensions"
            ]
        }
    ]
}
